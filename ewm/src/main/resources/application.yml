server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:ewm
    username: test
    password: test
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

app:
  minTimeToEventMinutes: 120
  name: ewm

stats-server:
  url: http://localhost:9090
  getPath: /stats
  postPath: /hit